# URL de ton API
PORT=3000

# Secret pour signer les JWT (generer une valeur unique, ne jamais committer le vrai secret)
JWT_SECRET=change-me-in-development

# Connexion PostgreSQL (doit correspondre a docker/docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=semoa
DB_PASSWORD=ssap
DB_NAME=mmorpgdb
